<section class="feature-cards" dir="rtl">
    <div class="container px-0 px-md-3">
        <div class="feature-cards__grid" aria-label="Feature cards">
            <a class="feature-card expertise" href="/admission/expertise">
                <span class="feature-card__icon oi oi-magnifying-glass" aria-hidden="true"></span>
                <div class="feature-card__text">
                    <div class="feature-card__title">کارشناسی آنلاین</div>
                    <div class="feature-card__desc">مشاوره و عیب‌یابی تخصصی هوشمند</div>
                </div>
            </a>
            <a class="feature-card repair" href="/admission/repair">
                <span class="feature-card__icon oi oi-wrench" aria-hidden="true"></span>
                <div class="feature-card__text">
                    <div class="feature-card__title">درخواست تعمیر</div>
                    <div class="feature-card__desc">کولر ، یخچال و بریزر ، لباسشویی و...</div>
                </div>
            </a>
        </div>
    </div>
</section>

<style>
    .feature-cards {
        margin-top: 10px;
    }

    .feature-cards__grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 14px;
        padding: 0 20px;
    }

    .feature-card {
        background: #ffffff;
        border-radius: 16px;
        box-shadow: 0 12px 30px rgba(0,0,0,0.12);
        padding: 24px 20px;
        text-decoration: none;
        color: #1a202c;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        gap: 16px;
        min-height: 160px;
        transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
        border: 1px solid rgba(0,0,0,0.04);
        position: relative;
        overflow: hidden;
    }

    .feature-card::before {
        content: "";
        position: absolute;
        inset: -2px;
        opacity: 0;
        transition: opacity 300ms ease;
        border-radius: 18px;
        z-index: 0;
    }

    .feature-card__icon,
    .feature-card__text {
        position: relative;
        z-index: 1;
    }

    .feature-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 20px 40px rgba(0,0,0,0.16);
    }

    .feature-card.expertise {
        background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 45%, #0ea5e9 100%);
        color: #ffffff;
        border-color: rgba(37, 99, 235, 0.18);
    }

    .feature-card.repair {
        background: linear-gradient(135deg, #ea580c 0%, #f97316 40%, #eab308 100%);
        color: #ffffff;
        border-color: rgba(234, 88, 12, 0.18);
    }

    .feature-card.expertise:hover { border-color: rgba(37, 99, 235, 0.35); }
    .feature-card.repair:hover { border-color: rgba(234, 88, 12, 0.35); }

    .feature-card.expertise::before {
        background: radial-gradient(1200px 300px at 50% 0%, rgba(255, 255, 255, 0.20), transparent 55%),
                    linear-gradient(135deg, rgba(255, 255, 255, 0.10), rgba(255, 255, 255, 0.02));
    }
    .feature-card.repair::before {
        background: radial-gradient(1200px 300px at 50% 0%, rgba(255, 255, 255, 0.22), transparent 55%),
                    linear-gradient(135deg, rgba(255, 255, 255, 0.10), rgba(255, 255, 255, 0.02));
    }
    .feature-card:hover::before { opacity: 1; }

    .feature-card.expertise .feature-card__icon {
        background: rgba(255, 255, 255, 0.18);
        color: #ffffff;
    }
    .feature-card.expertise:hover .feature-card__icon {
        background: rgba(255, 255, 255, 0.28);
        color: #ffffff;
    }

    .feature-card.repair .feature-card__icon {
        background: rgba(255, 255, 255, 0.18);
        color: #ffffff;
    }
    .feature-card.repair:hover .feature-card__icon {
        background: rgba(255, 255, 255, 0.28);
        color: #ffffff;
    }

    .feature-card__icon {
        width: 60px;
        height: 60px;
        border-radius: 18px;
        background: linear-gradient(135deg, rgba(47, 178, 106, 0.1), rgba(47, 178, 106, 0.2));
        color: #2fb26a;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: transform 300ms;
    }

    .feature-card:hover .feature-card__icon {
        transform: scale(1.1) rotate(5deg);
        background: rgba(255, 255, 255, 0.28);
        color: #ffffff;
    }

    .feature-card:active {
        transform: translateY(-2px) scale(0.985);
    }

    .feature-card__icon ::deep .oi {
        font-size: 1.8rem;
    }

    .feature-card__title {
        font-weight: 800;
        font-size: 1.1rem;
        margin: 0;
        line-height: 1.4;
    }

    .feature-card__desc {
        font-size: 0.9rem;
        color: rgba(255,255,255,0.88);
        margin: 0;
    }

    @@media (max-width: 899.98px) {
        .feature-cards {
            margin-top: 15px;
        }

        .feature-cards__grid {
            padding: 0 8px;
            gap: 10px;
        }

        .feature-card {
            padding: 16px 10px;
            min-height: 120px;
            gap: 8px;
            border-radius: 12px;
        }

        .feature-card__icon {
            width: 42px;
            height: 42px;
            border-radius: 10px;
        }

        .feature-card__icon ::deep .oi {
            font-size: 1.2rem;
        }

        .feature-card__title {
            font-size: 0.9rem;
        }

        .feature-card__desc {
            font-size: 0.78rem;
        }
    }

    @@media (min-width: 900px) {
        .feature-cards__grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 28px;
            padding: 0 40px;
        }

        .feature-card {
            padding: 36px 28px;
            min-height: 220px;
            border-radius: 22px;
        }

        .feature-card__icon {
            width: 72px;
            height: 72px;
            border-radius: 22px;
        }

        .feature-card__title {
            font-size: 1.25rem;
        }

        .feature-card__desc {
            font-size: 0.95rem;
        }
    }
</style>
